name: CI

# Gate for the org tooling repo. The only build artifact here is the `labs`
# multi-repo management script, so "build + test" means: it parses, and it
# passes shellcheck. Runs on every PR (no path filter) so the required `ci`
# check always reports a result and never leaves a PR stuck "Expected".

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # No `name:` — the check run must report as `ci` to satisfy the required
  # status check of the same name in branch protection. A display name overrides
  # the check-run name and leaves every PR stuck "Expected — ci".
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Syntax check (bash -n)
        run: bash -n bin/labs

      - name: ShellCheck
        uses: ludeeus/action-shellcheck@master
        with:
          scandir: ./bin
